# Malicious URL Detection — dependencies
# Tested versions in comments; >= used for portability.

# --- Core data / ML ---
pandas>=2.0            # tested 3.0.3
numpy>=1.24           # tested 2.4.6
scipy>=1.10           # tested 1.17.1
scikit-learn>=1.3     # tested 1.8.0  (TfidfVectorizer, LinearSVC, metrics)
joblib>=1.3           # tested 1.5.3  (model persistence)

# --- URL parsing & tokenization ---
tldextract>=5.0       # tested 5.3.1  (Public Suffix List → correct TLD/registered domain)
wordsegment>=1.3      # tested 1.3.1  (split concatenated English tokens)
jieba>=0.42           # tested 0.42.1 (Chinese word segmentation)
langid>=1.1           # tested 1.1.6  (language detection → route to translator)

# --- Multilingual translation (decode → translate non-English URL fields) ---
transformers>=4.40    # tested 5.9.0  (Helsinki-NLP/opus-mt-mul-en)
torch>=2.0            # tested 2.12.0
sentencepiece>=0.2    # tested 0.2.1
sacremoses>=0.1       # tested 0.1.1

# --- Notebook & visualization ---
matplotlib>=3.7       # tested 3.10.9
jupyter
nbformat>=5.0
